---
name: global-api-tester
description: Use when editing global-api-tester. GET /test?url=; returns status, latency, ok; no bindings.
---

# Global API Tester

- **Route**: `GET /test?url=`
- **Outputs**: `status`, `latency`, `ok`, optional `error`.
