---
name: website-to-api
description: Use when editing website-to-api. GET /api?url=; returns structured title, headings, links, images.
---

# Website to API

- **Route**: `GET /api?url=`
- **Outputs**: `title`, `headings`, `links`, `images`.
