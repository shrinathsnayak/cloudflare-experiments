---
description: Contract for website-metadata-extractor (route, params, response)
globs: apps/experiments/website-metadata-extractor/**/*.ts
alwaysApply: false
---

# Website Metadata Extractor

- **Route**: `GET /metadata?url=<url>`.
- **Response**: `{ title, description, canonical, og: { ... } }`.
- **Bindings**: None. Uses fetch + HTML parsing (regex).
